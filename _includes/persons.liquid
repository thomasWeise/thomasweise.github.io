{%- assign array = "" | split: "," -%}
{%- for person in include.persons -%}
    {%- assign personstr=site.data.people[person] -%}
    {%- assign array = array | concat: personstr -%}
{%- endfor -%}
{%- include print_array.liquid array=array -%}
