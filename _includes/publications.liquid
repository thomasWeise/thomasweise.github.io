{%- capture pubtext -%}
    {%- for paper in include.papers -%}
        {%- include publication.liquid paper=paper -%}
    {%- endfor -%}
{%- endcapture -%}
<ul class="publist">
    {{- pubtext | remove: '<p>' | remove: '</p>' | strip | normalize_whitespace | replace: " ,", "," -}}
</ul>
