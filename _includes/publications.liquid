{%- capture pubtext -%}
{%- for paper in include.papers -%}
<li>{%- include publication.liquid paper=paper -%}</li>
{%- endfor -%}
{%- endcapture -%}
<ol markdown="0">{{- pubtext | remove: '<p>' | remove: '</p>' | strip | normalize_whitespace | replace: " ,", "," -}}</ol>
