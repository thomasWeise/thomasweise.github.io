{%- capture pubtext -%}
{%- for paper in include.papers -%}
<li>{%- include publication.liquid paper=paper -%}</li>
{%- endfor -%}
{%- endcapture -%}
<ul class="publist">{{- pubtext | remove: '<p>' | remove: '</p>' | strip | normalize_whitespace | replace: " ,", "," -}}</ul>
